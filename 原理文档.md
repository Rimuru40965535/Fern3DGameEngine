# 原理文档

终于来到了这一次重构的重头戏喵。  
在这里我会尽量详细地记录从向量开始到可用渲染管线之间的所有用到的数学原理，顺便记录一下实现细节喵。  
之前我用的投影矩阵，明明是我自己写的，现在我已经记不清我采用的坐标系到底是啥样了喵。

---
---

## 向量

向量部分大多来自高中数学喵。  
在高考中由大量的关于向量的知识，在此处不再赘述，只做必要说明喵。  
在程序处理的时候，我利用向量的正交分解，将向量存储为x、y、z三个轴的分量喵。  
我默认三个轴构成笛卡尔右手坐标系，并且y轴指向天喵。  
虽然大肥鱼告诉我计算机图形学一般会采取左手系，但是我个人还是偏向于使用右手系喵。  
~~（原因就是，我所有的数学运算都是在右手系完成的，在左手系的运算会导致我需要在很多地方加上一个负号，比如外积，而我在debug的时候不一定立马能想起来数学实现有bug）~~  

在实际的空间中，如果将相等的向量视作同一个向量的话，一个向量唯一对应空间中的一个点，所以空间点将会直接复用向量类。  

---

高中数学的向量没有涉及向量外积。  
~~当年在高二的时候只是听了课代表向我介绍外积的定义，我能从头到尾独立推出外积的所有常用性质以及分量计算法，然后我们一个班的人都在考场上这么找法向量。~~  

外积是一个运算，以 $ \times $ 表示，算符左右各接收一个向量，返回的向量具有如下性质：  

- **模长性质**：模长为两向量模长相乘，再乘以两向量夹角的 *正弦值*   
- **方向性质**：方向与两向量均垂直
- **定解条件**：算符左侧向量、算符右侧向量以及该向量三者可以张成空间右手坐标系。  

C++中有一个非常良好的特性叫运算符的重载。它支持同一个运算符根据两侧的接收参数不同执行不同的功能，外积也是依靠这个特性实现的。  
参阅：*API文档：Vector.h：operator%、operator%=*

---
---

## 矩阵

矩阵部分大多来自大学所学的线性代数喵。  
课上讲的东西无聊到让我项睡觉 ~~，但是我自己下来补齐了这一块，并且还形成了一套自己的理解~~喵。  
所以相比于标准的教材以及术语，这一块的内容会更多偏向于个人的理解喵。  

### 为啥一定要用矩阵处理空间变换？
因为对我而言，用矩阵处理空间变换最直观喵。  
给定一个向量，左乘一个矩阵，变成一个新的向量，这套链路对我而言很熟悉。  
我可以直接拿来用。  
虽然四元数也可以用来处理空间变换，虽然我以前在推导空间外积的时候想过有没有在三维空间里面把三维向量旋转的复数，也摸到了四元数的门，但是我现在没有完全掌握四元数。

### 为啥矩阵是4维的？

原因有很多，但是个人认为最主要的一个是三维的矩阵没有办法处理向量的平移。  
向量运算永远离不开平移和旋转。
如果拿最特殊的向量做一遍矩阵乘法……  

$ \left[ \begin{matrix} 1 & 2 & 3 \\ 4 & 5 & 6 \\ 7 & 8 & 9  \end{matrix} \right] \left[ \begin{matrix} 0 \\ 0 \\ 0   \end{matrix} \right] = \left[ \begin{matrix} 0 \\ 0 \\ 0   \end{matrix} \right]  $  

这就是问题。  

如果存在某一个三维矩阵可以完成对三维向量的平移，那么这个矩阵必然会将 $ [0 \  0\  0]^{T} $ 映射到别的位置上去。  
显然刚刚的运算说明了这个事情是不可能的。  

所以回想一下向量平移在做一件什么事情。它其实就是给向量的每一个分量加上一个 *常数* 。  
这个 *常数* 和给定向量的无论哪一个分量都无关。  
然而三维矩阵的运算过程注定了矩阵中的每一个数字都会或多或少与给定向量产生关联，在结果上是不会反映为常数的。  
所以矩阵必然要单开一列，与此同时向量必须要单开一行。  

为了连续应用变换，矩阵运算必须保证变换前和变换后都是四维向量（也就是保证矩阵变换对于向量封闭），必然要求矩阵再单开一行。

现在矩阵变成4*4了，向量也变成四维向量了。

这样做有一个明显的好处：所有三维矩阵可以表示的变换可以用四维矩阵表示，平移也可以用四维矩阵表示。变换的表现形式被统一了。

### 为啥向量的第四个坐标必然是1

其实数学上没有要求这里必须是1。三维的点可以等价于四维的一组共线向量。  
不过写成1的话会很方便。这样前三个值就是三维坐标点，见数读数就行了。  
我更关心的其实是这里应不应该是0。如果是0，那么它就不应该被平移影响。如果不是0而是其他数，那么就应该被平移影响。1是一个代表。  
最后，渲染管线会做一次除法，最终还会把坐标归一化回1。

### 为啥要把运算符 `*` 重载为左乘？

这和矩阵应用到向量变换次序不能随意变换以及 C++ 的运算符运算优先级有关。

矩阵的连锁变换是由右向左依次被应用到向量上的。比如：  

$  \mathcal{P} \cdot \mathcal{V} \cdot \mathcal{M} \cdot \vec{\mathcal{v}} $

就表示对向量 $ \vec{\mathcal{v}} $  依次应用 $ \mathcal{M} $、 $ \mathcal{V} $、 $ \mathcal{P} $ 三个矩阵。  
引用 3Blue1Brown 的一句话来形容就是：  

"Good news for my Herbrew readers, bad news for the rest of us."

~~C++ 肯定不说阿拉伯语或者希伯来语啊。~~  
C++ 的代码不支持我真的把三个矩阵依次排开，像这样：
``` 
render_dots[_] = project_matrix * view_matrix * model_matrix * cube.vertices[_];
```
然后不套括号，硬生生把矩阵的运算顺序变成从右到左，最后再把矩阵乘法乘出来的结果应用到向量上。  
强行这样做只会得到一个错误的变换。  
期望这样写的时候，其实是在期望 `project_matrix` 调用右乘的函数将 `view_matrix` 作为参数获取结果，然后在此之上继续调用右乘。  
我希望程序可以以一种我我可以理解的原理与形式运行。

所以我将 `*` 重载为左乘，并且实际使用的时候这样使用：
``` 
render_dots[_] = (model_matrix * view_matrix * project_matrix) * cube.vertices[_];
```
这样写我可以一眼看出来喂给向量的是什么变换，可以一眼看出来矩阵之间的相乘顺序，同时也可以保证电脑也按照我期望的顺序去做数学运算。  
左转的红灯最难等，数学的bug最难de  


---
---

## 渲染流程的数学原理

渲染的核心就是决定屏幕上的哪一个像素点应该显示什么颜色喵。  
所以只要从数学上推算穿过屏幕上给定点到达人眼的光线带有什么颜色，就能知道这个给定点应该被渲染成什么颜色喵。  
好在我的引擎不涉及引力导致的光线路径弯曲，所以所有的光线都是走直线的喵。

### 一个模型从定义到屏幕上的像素点需要哪些步骤，哪些矩阵？

模型的定义是在模型的坐标系（空间）下定义的，而渲染的时候需要在屏幕坐标系里面渲染。  

一次矩阵变换，就是一次空间的转换。  

首先，投影后得到的空间需要变成屏幕空间才能被渲染到屏幕上。  
然后，需要一个相机进行投影，把相机空间的世界投影到一个空间。  
第三，必须要知道相机被放在世界的位置，指向什么方向，上在哪里，才能把世界空间变换到相机空间里面。  
第四，模型在模型空间被定义，同一个模型的不同实例也可能被放置在不同的位置，所以还需要一个矩阵架起从模型空间到世界空间的桥梁。

所以一个模型从定义到屏幕上的坐标，至少需要4个矩阵
-
- **模型矩阵**：将模型的定义从模型空间搬到世界空间。
- **观察矩阵**：将世界空间的点搬到相机空间。
- **投影矩阵**：将相机空间的点搬到 *NDC* 空间里面（大肥鱼告诉我的名字，全称是”归一化设备坐标“）。
- **视窗变换**：将 NDC 空间里面的点变成屏幕空间里面的点。

做完以上数学变换，就可以开始在屏幕上绘制了。

### 各个矩阵的定义是怎样的？

模型矩阵和观察矩阵本质上都是空间变换。  
我希望所有的空间都是笛卡尔右手坐标系。  

**模型矩阵**负责把模型定义所在的空间的基底变换到世界空间的基底。它只是一个空间变换。  
其左上角的 3\*3 矩阵负责旋转、缩放、剪切等等的线性变换，右上角的 3\*1 向量负责平移，左下角的 1\*3 向量填充 0 ，右下角的变量填充 1 ，
这样保证向量的齐次坐标w不变。

**观察矩阵**负责把世界空间变换到相机空间，因此实际上 *观察矩阵是相机的模型矩阵的逆* 。

**投影矩阵**负责把相机空间变换到 NDC 空间。  
开发到这里的时候我会在*此处*补充关于投影矩阵的定义详情。

**视窗矩阵**负责把NDC空间变换到屏幕空间，屏幕在此结果之上应用各种渲染算法渲染。  
考虑到Eazyx对窗口的坐标定义是：  

- 原点在最左上角的像素中心
- x轴正方向是向屏幕右边的
- y轴正方向是向屏幕下边的

同时考虑到我的坐标是右手系，我的屏幕视窗坐标系就应该把 **z轴正方向设定为穿入屏幕** 。
