# 原理文档

终于来到了这一次重构的重头戏喵。  
在这里我会尽量详细地记录从向量开始到可用渲染管线之间的所有用到的数学原理，顺便记录一下实现细节喵。  
之前我用的投影矩阵，明明是我自己写的，现在我已经记不清我采用的坐标系到底是啥样了喵。

---

## 向量

向量部分大多来自高中数学喵。  
在高考中由大量的关于向量的知识，在此处不再赘述，只做必要说明喵。  
在程序处理的时候，我利用向量的正交分解，将向量存储为x、y、z三个轴的分量喵。  
我默认三个轴构成笛卡尔右手坐标系，并且y轴指向天喵。  
虽然大肥鱼告诉我计算机图形学一般会采取左手系，但是我个人还是偏向于使用右手系喵。  
~~（原因就是，我所有的数学运算都是在右手系完成的，在左手系的运算会导致我需要在很多地方加上一个负号，比如外积，而我在debug的时候不一定立马能想起来数学实现有bug）~~  

在实际的空间中，如果将相等的向量视作同一个向量的话，一个向量唯一对应空间中的一个点，所以空间点将会直接复用向量类。  

---

高中数学的向量没有涉及向量外积。外积是一个运算，以 $ \times $ 表示，算符左右各接收一个向量，返回的向量具有如下性质：  

- 模长为两向量模长相乘，再乘以两向量夹角的 *正弦值*   
- 方向与两向量均垂直
- 算符左侧向量、算符右侧向量以及该向量三者可以张成空间右手坐标系。  

C++中有一个非常良好的特性叫运算符的重载。它支持同一个运算符根据两侧的接收参数不同执行不同的功能，外积也是依靠这个特性实现的。  
*参阅 API文档：Vector.h：operator%、operator%=*

---

